{
  "name": "ambient-music-vs-extension",
  "displayName": "Ambient Music AutoPlayer by Taj",
  "icon": "media/logo.png",
  "description": "Auto-play ambient music when a VS Code project opens.",
  "version": "1.2.46",
  "publisher": "taj154dev",
  "repository": {
    "type": "git",
    "url": "https://github.com/taj54/ambient-music-vs-extension.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "main": "./dist/src/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "workspaceContains:**/*"
  ],
  "contributes": {
    "commands": [
      {
        "command": "ambientMusic.setPlaylist",
        "title": "ambient-taj: Set Playlist"
      },
      {
        "command": "ambientMusic.openTab",
        "title": "ambient-taj: Open Ambient Music Tab"
      },
      {
        "command": "ambientMusic.play",
        "title": "ambient-taj: Play Ambient Music"
      },
      {
        "command": "ambientMusic.pause",
        "title": "ambient-taj: Pause Ambient Music"
      },
      {
        "command": "ambientMusic.resume",
        "title": "ambient-taj: Resume Ambient Music"
      },
      {
        "command": "ambientMusic.closeTab",
        "title": "ambient-taj: Close Ambient Music Tab"
      }
    ],
    "configuration": {
      "title": "Ambient Music Settings",
      "properties": {
        "ambientMusic.port": {
          "type": "number",
          "default": 0,
          "description": "Port for the ambient music WebSocket server. Set to 0 for dynamic port."
        },
        "ambientMusic.switchIntervalMinutes": {
          "type": "number",
          "default": 30,
          "description": "How often (in minutes) to switch to the next ambient video from the playlist."
        },
        "ambientMusic.debug": {
          "type": "boolean",
          "default": false,
          "description": "Enable verbose debug logging for Ambient Music."
        },
        "ambientMusic.playlist": {
          "type": "array",
          "description": "Custom playlist of YouTube URLs to play.",
          "items": {
            "type": "string"
          },
          "default": []
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node ./dist/test/runTest.js"
  },
  "devDependencies": {
    "@types/chai": "^5.2.2",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.0.4",
    "@types/vscode": "^1.80.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-electron": "^2.5.2",
    "chai": "^5.2.0",
    "mocha": "^11.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "vsce": "^2.15.0",
    "vscode-test": "^1.6.1"
  },
  "dependencies": {
    "ws": "^8.18.2"
  }
}